/*
  Warnings:

  - The primary key for the `HelpThread` table will be changed. If it partially fails, the table could be left without primary key constraint.

*/
-- AlterTable
ALTER TABLE `HelpThread` DROP PRIMARY KEY;
